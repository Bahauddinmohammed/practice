import { DdDirective } from './dd.directive';

describe('DdDirective', () => {
  it('should create an instance', () => {
    const directive = new DdDirective();
    expect(directive).toBeTruthy();
  });
});
